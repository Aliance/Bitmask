build:
  environment:
    php:
      version: 8.3
  tests:
    override:
      - command: './vendor/bin/phpunit --coverage-clover report.xml'
        coverage:
          file: 'report.xml'
          format: 'clover'
